# Task: Wallet Features & Fixes

## Balance Update (Completed)
- [x] Refactor `TaskCenter.js` to use local state for instant UI updates.
- [x] Sync `earnings` and `completed` count from `onComplete` callback.

## Wallet Transfer (Income -> Main)
- [ ] Locate/Create Transfer UI in `frontend/app/wallet`.
    - Key phrase: "quick wallet" or "income wallet".
- [ ] Implement/Verify `transferToMain` API call.
- [ ] Ensure user can select "Income Wallet" and transfer to "Main Wallet".
